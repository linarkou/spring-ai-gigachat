spring:
  ai:
    gigachat:
      auth:
        bearer:
          client-id: ${GIGACHAT_API_CLIENT_ID}
          client-secret: ${GIGACHAT_API_CLIENT_SECRET}
        scope: ${GIGACHAT_API_SCOPE}
        unsafe-ssl: true
