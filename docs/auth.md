# GigaChat Аутентификация: Использование GigaAuthToken

`GigaAuthToken` — это интерфейс, предназначенный для динамического получения и обновления токенов аутентификации
при взаимодействии с API **GigaChat**. Он позволяет реализовать гибкие сценарии аутентификации, включая использование
**временных (время жизни ограничено) токенов**, **автоматическое обновление** и
**интеграцию с внешними системами управления доступом**.

## Когда использовать GigaAuthToken?

Используйте `GigaAuthToken`, если:
- Вам нужно работать с **динамически обновляемыми токенами** (например, OAuth2-токенами).
- Токен имеет ограниченное время жизни (TTL) и требует **периодического обновления**.
- Вы интегрируетесь с системой, где ключи/токены управляются централизованно (например, HashiCorp Vault, Kubernetes Secrets, IAM-сервисы).
- Вы хотите избежать хранения статичных секретов в конфигурации.

## Как работает GigaAuthToken?

Интерфейс `GigaAuthToken` определяет единственный метод:

```java
String getValue();
```

Этот метод вызывается каждый раз перед отправкой запроса авторизации к GigaChat API, что гарантирует использование **актуального токена**.

Реализуйте интерфейс `GigaAuthToken` в своём Spring-компоненте.

## Настройка в Spring-приложении

После реализации бина `GigaAuthToken`, зарегистрируйте его в контексте Spring одним их следующих способов:
- через фабричный метод, аннотированый `@Bean`
- через сканирование классов с аннотацией `@Component`.

## Преимущества использования GigaAuthToken

- ✅ **Безопасность**: ключи не хранятся долго в памяти.
- ✅ **Автоматизация**: токены обновляются прозрачно для API-клиента.
- ✅ **Гибкость**: можно интегрироваться с любым провайдером токенов.
- ✅ **Поддержка продвинутых сценариев**: MFA, федеративная аутентификация, ротация ключей.

## Связанные интерфейсы

- `ApiKey` — общий интерфейс Spring AI для ключей. `GigaAuthToken` может быть обёрнут в `ApiKey`, если используется в совместимом контексте.
- `GigaChatAuthProperties` — класс конфигурации, который может принимать `GigaAuthToken`.

---

> 💡 **Совет**: Всегда добавляйте буфер времени (например, 30–60 секунд) до истечения срока действия токена, чтобы
> избежать ошибок из-за сетевой задержки.

